# Jezweb MCP Core - Environment Configuration
# Copy this file to .env and fill in your actual values

# Required: OpenAI API Key
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# Optional: Server Configuration
# Customize the MCP server name (default: jezweb-mcp-core)
MCP_SERVER_NAME=jezweb-mcp-core

# Customize the MCP server version (default: 3.0.1)
MCP_SERVER_VERSION=3.0.1

# Optional: Debug Configuration
# Enable debug logging (default: false)
DEBUG=false
MCP_DEBUG=false

# Optional: Advanced Configuration
# These are typically not needed for basic usage

# Custom OpenAI API base URL (for proxies or custom endpoints)
# OPENAI_API_BASE=https://api.openai.com/v1

# Request timeout in milliseconds (default: 30000)
# REQUEST_TIMEOUT=30000

# Maximum retries for failed requests (default: 3)
# MAX_RETRIES=3

# Rate limiting configuration
# RATE_LIMIT_REQUESTS_PER_MINUTE=60

# Example Usage:
# 1. Copy this file: cp .env.example .env
# 2. Edit .env and add your OpenAI API key
# 3. Run: npm start
# 4. Or use with MCP clients like Claude Desktop or Roo